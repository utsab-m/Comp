#include "bits/stdc++.h"
using namespace std;
using ll = long long;

const int MOD = 1e9+7;

struct SegTree {
    vector<int> tree;

    SegTree(int n) {
        tree.resize(n+1);
        
    }

    void build(int node, int l, int r) {
        if (l == r) {
            tree[node] = 
        }
    }
};

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    
}