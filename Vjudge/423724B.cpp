#include "bits/stdc++.h"
using namespace std;
using ll = long long;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int a, b, c;
    double x, y;
    cin >> a >> b >> c >> x >> y;

    cout << 2 * a * x + b << "\n";
}