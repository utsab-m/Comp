#include <iostream>
#include <cmath>
#include <cstdlib>

bool perfect(int n) {
    int root = (int)sqrt(n);
    return root * root == n;
}

int main(int argc, char *argv[]) {
    if ()
    std::cout << "-1" << std::endl;
    return 0;
}